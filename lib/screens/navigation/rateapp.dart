import 'package:flutter/material.dart';

class Rateapp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Rate App'),
        centerTitle: true,
      ),
      body: Text('Google play rate link and also share app'),
    );
  }
}
